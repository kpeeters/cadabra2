
- xcode
- homebrew
- mactex
- brew install cmake
- brew install boost
- brew install pcre
- brew install gmp (still needs cmake test!)

server/client:
- brew install zmq


gtkmm client:
- brew tap acornejo/quartz
- brew install gtkmm-quartz

to compile cadabra (v1.*), 

 export PKG_CONFIG_PATH=/usr/local/opt/gtkmm-quartz/lib/pkgconfig:/usr/local/opt/gtk+-quartz/lib/pkgconfig:/usr/local/opt/pango-quartz/lib/pkgconfig:${PKG_CONFIG_PATH}
 ./configure --disable-runtime-dependency-check

 gtk+-quartz
 pango-quartz
 pangomm-quartz
 gtkmm-quartz
 cairo-quartz
 cairomm-quartz
